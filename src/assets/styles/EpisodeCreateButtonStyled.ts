import styled from '@emotion/styled'
import { Fab } from '@mui/material'

const EpiCreateButton = styled(Fab)`
  position: fixed;
  right: 32px;
  bottom: 32px;
  z-index: 999;
`

export default EpiCreateButton
